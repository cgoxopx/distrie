#ifndef distrie_config
#define distrie_config
#include <stdlib.h>
#include <stdio.h>
#include <string>
namespace distrie{
    class Config{
        public:
            int chunkServerNum;
            int backupNum;
    }config;
}
#endif